
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">
  <style>
    body {
      background-image: linear-gradient(to right, #f8f9fa, #70ace9);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    h1 {
      font-size: 36px;
      font-weight: bold;
      color: #333;
      margin-bottom: 40px;
      text-shadow: 2px 2px #ddd;
    }

    .btn-primary, .btn-secondary {
      font-size: 18px;
      font-weight: bold;
      padding: 10px 40px;
      border-radius: 30px;
      box-shadow: 0px 5px 10px rgba(0,0,0,0.1);
      transition: all 0.3s ease-in-out;
    }

    .btn-primary:hover, .btn-secondary:hover {
      transform: translateY(-3px);
      box-shadow: 0px 10px 20px rgba(0,0,0,0.2);
    }

    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
      color: #fff;
    }

    .btn-secondary {
      background-color: #6c757d;
      border-color: #6c757d;
      color: #fff;
    }
  </style>
  <title>Task Manager</title>
</head>
<body>
  <div class="container my-5">
    <h1 class="text-center">Task Manager Admin Panel</h1>
      <form class="d-flex justify-content-center">
          <input type="submit" value="Assign Task" class="btn btn-primary mx-3" onclick="submitForm('/main', 'post', this.form)">
          <input type="submit" value="View Task" class="btn btn-primary mx-3" onclick="submitForm('/admin-task', 'post',this.form)">
          <input type="submit" value="Signout" class="btn btn-primary mx-3" onclick="submitForm('/login', 'post',this.form)">
          <!-- <input type="submit" value="Signout" class="btn btn-primary mx-3" onclick="Signout()"> -->
      </form>
  </div>
</body>
<script>
  function submitForm(action, method, form) {
    form.action = action;
    form._method.value = method;
    form.submit();
  }

</script>
</html>


